/// <reference types="react" />
import { ActionMeta, OnChangeValue } from "react-select";
import { Option, SelectProps } from "./CommonSelect";
declare type EstadosOption = Option & {
    ibge?: number;
};
declare type EstadosSelectProps = Omit<SelectProps, "onChange"> & {
    onChange: (newValue: OnChangeValue<EstadosOption, false>, actionMeta: ActionMeta<EstadosOption>) => void;
};
export declare const EstadosOptions: {
    value: string;
    label: string;
    ibge: number;
}[];
export declare const Estados: (props: EstadosSelectProps) => JSX.Element;
export {};
