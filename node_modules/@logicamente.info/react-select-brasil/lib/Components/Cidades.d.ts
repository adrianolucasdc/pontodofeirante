/// <reference types="react" />
import { ActionMeta, OnChangeValue } from "react-select";
import { Option, SelectProps } from "./CommonSelect";
export declare type EstadosProps = keyof typeof Brasil;
declare type CidadesOption = Option & {
    ibge?: number | null;
};
declare type CidadesSelectProp = Omit<SelectProps, "onChange"> & {
    estado?: EstadosProps | null;
    onChange: (newValue: OnChangeValue<CidadesOption, false>, actionMeta: ActionMeta<CidadesOption>) => void;
};
export declare const Cidades: ({ estado, ...props }: CidadesSelectProp) => JSX.Element;
interface IBrasil {
    [key: string]: CidadesOption[];
}
export declare const Brasil: IBrasil;
export {};
